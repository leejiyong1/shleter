<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "HTTP://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.project.mapper.CommentMapper">

<select id="comment_list" resultType="com.animal.shelter.vo.Comment" parameterType="java.util.HashMap">
		SELECT * FROM (SELECT RANK() OVER( ORDER BY c_idx DESC) no, c.*
	         		   FROM comment c WHERE b_idx = #{b_idx})
		WHERE no BETWEEN #{start} AND #{end}
	</select>
	
	<select id="comment_count" resultType="int">
		SELECT count(*) FROM comment WHERE b_idx = #{b_idx}
	</select>
</mapper>